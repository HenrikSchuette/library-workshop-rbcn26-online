# Task 2: Library Components

- Extract the API call keywords to a separate Python module and class.
- Create a keyword called 'get_todos' that returns all existing 'todos'.
- Create a Robot Framework test that uses the new keyword to verify that the created task exists.