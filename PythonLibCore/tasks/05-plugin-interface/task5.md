# Task 5: Plugin Interface

- Make the library extensible by enabling users to create and add their own library components.
- Create a plugin for handling teardown operations. This plugin should contain a keyword that deletes all 'todos' created after test execution.
- Pass the plugin to the library and ensure that the teardown keyword is executed after the tests.